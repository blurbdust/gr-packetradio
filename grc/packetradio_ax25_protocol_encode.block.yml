# auto-generated by grc.converter

id: packetradio_ax25_protocol_encode
label: AX.25 Protocol Encoder
category: '[packetradio]'

parameters:
-   id: scall
    label: Source Callsign
    dtype: string
    default: NOCALL
-   id: dcall
    label: Destination Callsign
    dtype: string
    default: CQ
-   id: shift_call
    label: Left Shift Callsign
    dtype: bool
    default: 'True'
-   id: control
    label: Control Byte
    dtype: int
    default: '0x03'

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import packetradio
    make: packetradio.ax25_protocol_encode(${dcall}, ${scall}, ${shift_call}, ${control})

file_format: 1
