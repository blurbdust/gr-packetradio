# auto-generated by grc.converter

id: packetradio_bell_202_modem
label: Bell 202 Modem
category: '[packetradio]'

parameters:
-   id: samp_rate
    label: Sample Rate
    dtype: int
    default: '38400'
-   id: baud
    label: Baud Rate
    dtype: int
    default: '1200'

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import packetradio
    make: packetradio.bell_202_modem(${samp_rate}, ${baud})

file_format: 1
